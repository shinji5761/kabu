import { Dao } from '../common/Dao';
import { Logger } from '../../../../common/logger/Logger';
const logger = new Logger('server');


export class UserDao extends Dao {

    public get(params, callback : Function) : void {

    }

}