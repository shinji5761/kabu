import { Service } from '../common/Service';
import { Logger } from '../../../../common/logger/Logger';
const logger = new Logger('server');

/**
 * @class OnlineService
 * @extends Service
 */
export class OnlineService extends Service {
    constructor() {
        super();
    }
}
